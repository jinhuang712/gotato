# Copy this file to gateway.yaml and fill in the deployment-specific values.
# ${ENV_VAR} references are expanded when the file is loaded.
api: openai-completions
endpoint: https://gateway.example.com/v1/chat/completions
# Alternatively use base_url: https://gateway.example.com/v1
api_key: ${GOTATO_GATEWAY_API_KEY}
model: your-model
max_retries: 2
retry_backoff: 200ms
headers:
  X-Client-Name: gotato-agent
